export const metadata = {
  title: 'Accounts',
  description: 'Manage your ledger accounts and view real-time balances.',
};

export default function AccountsLayout({ children }) {
  return <>{children}</>;
}
