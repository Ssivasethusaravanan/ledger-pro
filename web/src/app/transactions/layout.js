export const metadata = {
  title: 'Transactions',
  description: 'View and manage double-entry ledger transactions and postings.',
};

export default function TransactionsLayout({ children }) {
  return <>{children}</>;
}
